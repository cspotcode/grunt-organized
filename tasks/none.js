module.exports = function(grunt) {
    /*
     * Do nothing.
     * 
     * This file exists to prevent [load-grunt-tasks](https://www.npmjs.com/package/load-grunt-tasks)
     * By default, it attempts to load each dependency named "grunt-*"
     * as a grunt plugin.  It will log an error if it can't
     * `grunt.loadTasks('grunt-organized/tasks')`
     * load-grunt-tasks can be configured to skip this module, but that
     * requires extra work, and it was easier to include this file.
     */
};